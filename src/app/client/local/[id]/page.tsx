// import { getRestaurants } from "@/services/client/client-service";
// import RestaurantDetailPage from "@/ui/client/restaurant/restaurant-detail-page";

// export default async function Page({
//   params,
// }: {
//   params: Promise<{ id: string }>;
// }) {
//   const { id } = await params;
//   const dish = await getRestaurants(id);
//   return <RestaurantDetailPage dish={dish} />;
// }